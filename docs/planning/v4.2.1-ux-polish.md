# v4.2.1 Implementation Plan - UX Polish

**Status:** Complete
**Created:** 2026-01-08
**Based on:** User feedback from v4.0.2 → v4.2.0 upgrade (personal-website project)

---

## Overview

This patch release addresses UX issues identified during a real upgrade experience. All issues are minor/cosmetic but affect user confidence during updates.

### Release Scope

- **2 Bug Fixes** - Confusing output during update process
- **0 Breaking Changes** - Fully backward compatible

### Success Criteria

- [x] Update notice suppressed during `/update-context-system`
- [x] Outdated "What's New" info updated or removed
- [x] Full test suite passes
- [ ] Upgrade path tested (manual verification recommended)

---

## Issues from User Feedback

### Issue 1: Update Notice Shows During Update (Priority: High)

**Source:** context-feedback.md - "common-functions.sh Auto-Shows Update Notice"

**Problem:** When sourcing `scripts/common-functions.sh` during `/update-context-system`, it displays:

```
💡 Update available: v4.0.2 → v4.2.0
   Run /update-context-system to upgrade
```

This is confusing because the user is ALREADY running `/update-context-system`.

**Location:** `scripts/common-functions.sh:514`

**Fix:** Add environment variable check to suppress notice during updates:

```bash
# At start of /update-context-system (Step 0)
export ACS_UPDATING=true

# In common-functions.sh check_for_updates() function
check_for_updates() {
  # Skip if we're already updating
  if [ "$ACS_UPDATING" = "true" ]; then
    return 0
  fi
  # ... existing logic
}
```

**Testing:**
- Run `/update-context-system` and verify no "Run /update-context-system" message appears
- Run `/review-context` and verify update notice DOES appear (when outdated)

---

### Issue 2: Outdated "What's New" Documentation (Priority: Medium)

**Source:** context-feedback.md - '"What's New" Section Hardcoded'

**Problem:** User reported seeing "What's New in v4.0.0" during upgrade to v4.2.0.

**Investigation:**
- `update-context-system.md` Step 3 was fixed in v4.1.0 to link to CHANGELOG
- However, `.claude/docs/update-guide.md` still shows "What's New in v3.3.0" (line 7)
- This file may be displayed during updates or referenced by users

**Locations to check:**
1. `.claude/docs/update-guide.md` - Line 7: "## 🎉 What's New in v3.3.0"
2. Any other hardcoded version strings

**Decision:** Remove `.claude/docs/update-guide.md` entirely.

**Rationale:**
1. **Maintenance burden** - 800 lines that need updating with every release
2. **Currently harmful** - Shows "What's New in v3.3.0" which confuses users at v4.2.x
3. **Redundant** - CHANGELOG.md and acs-docs migration guide cover this better
4. **Bloat** - Gets copied to every user project via `.claude/docs/`
5. **Outdated mechanics** - Describes workflows that may not match current behavior

**What to preserve:** Review for any still-valid troubleshooting tips before deletion. If found, add to TROUBLESHOOTING.md.

**Fix:**
```bash
rm .claude/docs/update-guide.md
```

**Testing:**
- Grep for any remaining hardcoded "What's New in v" strings
- Verify update flow doesn't reference removed file

---

## Issues Noted But Not Fixing (This Release)

### Issue 3: Inconsistent Archive Locations
- Multiple archive locations exist (`.archive/`, `artifacts/feedback/`, `docs/audits/archive/`)
- **Decision:** Document only, not a bug
- **Future:** Consider standardization in v5.0

### Issue 4: Multi-AI Templates Not Documented
- New templates exist but no workflow docs
- **Decision:** Documentation task, not code fix
- **Future:** Add to README or create `/setup-ai-headers` command

### Issue 5: Copy-Paste Git Prompt Redundant
- Same long prompt in multiple commands
- **Decision:** Leave as-is per user preference
- **Future:** Consider consolidating in v5.0

---

## Implementation Checklist

### Phase 1: Bug Fixes

- [x] **Fix 1:** Add `ACS_UPDATING` suppression
  - [x] Add export at start of `update-context-system.md`
  - [x] Add check in `common-functions.sh` `check_for_updates()`
  - [x] Test: update runs without "Run /update-context-system" message

- [x] **Fix 2:** Remove outdated update-guide.md
  - [x] Review file for any still-valid troubleshooting tips
  - [x] Remove `.claude/docs/update-guide.md`
  - [x] Update install.sh if it references this file
  - [x] Grep for any other hardcoded version strings
  - [x] Test: no outdated "What's New" displayed

### Phase 2: Testing

- [x] Run full test suite (78 tests passed)
- [ ] Test upgrade from v4.2.0 → v4.2.1 (manual)
- [ ] Verify update notice works correctly in `/review-context` (manual)

### Phase 3: Release

- [x] Update VERSION file to 4.2.1
- [x] Update CHANGELOG.md
- [ ] Update command footers (if needed)
- [ ] Commit and tag release

---

## Notes

- This is a minor polish release focused on update UX
- No functional changes to core commands
- Backward compatible with v4.2.0
